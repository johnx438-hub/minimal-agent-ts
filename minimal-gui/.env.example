# Optional OpenAI (assistant-ui templates may use it — not required for minimal Route A)
# OPENAI_API_KEY=

# minimal-agent-ts Web UI (npm run web -- --web-port 7788)
NEXT_PUBLIC_MINIMAL_BASE_URL=http://127.0.0.1:7788
# Paste token printed by the web server, or open GUI with ?token=
NEXT_PUBLIC_MINIMAL_TOKEN=
